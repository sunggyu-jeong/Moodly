{
  "name": "moodly-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:mobile": "pnpm --filter mobile start",
    "dev:ios": "pnpm --filter mobile exec -- npx expo run:ios",
    "dev:android": "pnpm --filter mobile exec -- npx expo run:android",
    "build:mobile:android": "pnpm --filter mobile exec -- eas build --platform android",
    "build:mobile:ios": "pnpm --filter mobile exec -- eas build --platform ios",
    "dev:jobs": "pnpm --filter jobs exec -- pnpm dlx trigger.dev@latest dev --env-file .env",
    "build:mobile:ios:adhoc": "pnpm --filter mobile exec -- eas build --platform ios --profile preview",
    "deploy:jobs": "pnpm --filter jobs exec -- npx trigger.dev@latest deploy"
  },
  "pnpm": {
    "overrides": {
      "@react-native-async-storage/async-storage": "2.2.0"
    }
  }
}